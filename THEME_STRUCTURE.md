# 📁 بنية مشروع Quiet Hour Theme

## الهيكل الكامل

```
quiet-hour-theme/
├── src/                              # كود المصدر
│   ├── assets/
│   │   ├── styles/app.css           # ملف CSS غير مضغوط
│   │   └── scripts/app.js           # ملف JavaScript الأصلي
│   ├── locales/ar.json              # الترجمات
│   └── views/                       # قوالب Twig
│       ├── layouts/master.twig      # هيكل الصفحات
│       ├── pages/                   # الصفحات (home, product, ...)
│       └── components/              # المكونات (global & home)
├── public/
│   ├── app.css                      # ناتج البناء النهائي
│   └── app.js
├── package.json / package-lock.json # سكربتات npm
├── twilight.json                    # تعريف المزايا والمكونات
├── README.md                        # وصف المشروع
└── *.md                             # وثائق إضافية
```

## 📝 الملفات الرئيسية

### 1. `twilight.json`
- بيانات الثيم الرسمية (الاسم، الوصف، الدعم)
- قائمة المزايا `features`
- إعدادات لوحة التحكم `settings`
- تعريف كل مكون ومجاله (home.*, global.*)

### 2. `src/views/layouts/master.twig`
- القالب الرئيسي لكل الصفحات
- يحمّل المكونات العالمية (Header / Footer / Mega Menu)
- يحقن ملفات `app.css` و `app.js`

### 3. `src/views/pages/index.twig`
- يستخدم `{% component home %}` لعرض مكونات الصفحة الرئيسية طبقاً لتعريف `twilight.json`

### 4. `src/views/pages/product/index.twig`
- قالب صفحة المنتج
- يعرض الصور، التفاصيل، السعر وزر الإضافة إلى السلة

### 5. `src/views/components/global/*`
- `global.header` : الشعار، القائمة، أيقونة السلة، تفعيل الميجا منيو
- `global.mega-menu` : عناصر القائمة المتفرعة
- `global.footer` : العناوين، الروابط، معلومات التواصل

### 6. `src/views/components/home/*`
- جميع مكونات الصفحة الرئيسية (Hero, Services, Photos Slider, ... إلخ)
- كل مكون مرتبط بمدخل في `twilight.json` (path = `home.component-name`)

### 7. `src/assets`
- `styles/app.css`: أنماط التصميم ومتغيرات الألوان
- `scripts/app.js`: الحركات، السلايدر، تأثيرات التمرير

### 8. `public/`
- يحتوي النسخة النهائية للملفات التي يتم رفعها للمتجر
- يتم توليدها عبر `npm run build`

## 🎨 الألوان المستخدمة

```css
--primary-color: #4A3728    /* البني الداكن */
--secondary-color: #E8DCC6  /* البيج الفاتح */
--background-color: #FFF8F0 /* الكريمي */
--text-color: #2C2416       /* النص الداكن */
--accent-color: #8B6F47     /* البني المتوسط */
```

## 🚀 الخطوات التالية

1. ✅ تثبيت الاعتمادات عبر `npm install`
2. ✅ بناء الملفات بأمر `npm run build`
3. ✅ تشغيل المعاينة باستخدام `salla theme serve`
4. ✅ توصيل الريبو مع Salla Partners Portal
5. ✅ تنفيذ `salla theme publish` بعد نجاح فحص TwilightCI

## 📚 المراجع

- [Salla Liquid Documentation](https://docs.salla.dev)
- [Liquid Template Language](https://shopify.github.io/liquid/)

